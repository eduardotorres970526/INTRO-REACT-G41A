import React from 'react'

const RestartButton = ({reestablecer}) => {
  return (
    <div>
        <button 
        onClick={reestablecer}>
        Reiniciar juego
        </button></div>
  )
}

export default RestartButton